<!DOCTYPE html>
<html>
<head>
  <title>Binge Pilot - Unreeling Inspiration</title>
  <meta name="description" content="Effortlessly unlock your next binge-worthy obsession. Swipe through tailored picks, find hidden gems, and dive into your next favorite movie or TV show with ease. Where your next watch is just a swipe away." />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <meta name="theme-color" content="#2F2C86">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Open Graph tags -->
  <meta property="og:image" content="<%= asset_path 'og_image.png' %>" />
  <meta property="og:title" content="Binge Pilot" />
  <meta property="og:description" content="Effortlessly unlock your next binge-worthy obsession." />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="<%= request.original_url %>" />

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= render 'shared/ios_pwa_links' %>
  <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
</head>
<body>
  <% unless controller_name == 'pages' && action_name == 'home' %>
    <%= render "shared/navbar" %>
  <% end %>
  <%= render "shared/flashes" %>

  <%= yield %>
</body>
</html>
